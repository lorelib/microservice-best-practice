spring:
  application:
    name: microservice-organization
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-server
      profile: dev
eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery:8010/eureka/